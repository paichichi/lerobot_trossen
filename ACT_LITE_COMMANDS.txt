# Run from the lerobot_trossen repository root.
# Console logs are saved under output/<model>_<UTC timestamp>/.
# Physical evaluation uses LeRobot base mode: no rollout dataset or video is recorded.
# Press Ctrl+C after the task; official teardown returns to the initial position.

# One-click cleanup if a previous LeRobot/Trossen rollout is still running.
bash scripts/cleanup_trossen_processes.sh

# Ours RN50: download and show the safety report (does not move the robot).
bash scripts/run_uploaded_act.sh ours_rn50

# Ours RN50: real-robot evaluation. E-stop must be ready.
bash scripts/run_uploaded_act.sh ours_rn50 --execute

# Official RN18 alternatives:
# bash scripts/run_uploaded_act.sh rn18
# bash scripts/run_uploaded_act.sh rn18 --execute

# V11: convert the trained checkpoint to the LeRobot policy contract (no robot motion).
bash scripts/run_uploaded_v11.sh

# V11: execute through the official LeRobot/Trossen rollout stack.
# bash scripts/run_uploaded_v11.sh --execute
