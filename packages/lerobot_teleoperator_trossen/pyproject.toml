[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lerobot_teleoperator_trossen"
version = "0.1.0"
description = "Trossen Robotics LeRobot integration - Teleoperators"
authors = [{ name = "Trossen Robotics" }]
license = "Apache-2.0"
license-files = ["LICENSE"]
readme = "README.md"
requires-python = ">=3.12,<3.13"
# See the robot package for why 0.6.0 and "core-scripts" are required.
dependencies = ["lerobot[intelrealsense,core-scripts]>=0.6.0", "trossen_arm==1.9.3"]

[tool.setuptools.packages.find]
where = ["src"]
include = ["lerobot_teleoperator_trossen"]
